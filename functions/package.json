{
  "name": "finpay-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  }
}
